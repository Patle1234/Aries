name: aries
description: A timekeeping app developed in Flutter. 

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
google_fonts:
version: 0.0.1+1
assets:
 -lib/images/apple.png
 -lib/images/google.png
environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  english_words: ^4.0.0
  provider: ^6.0.0
  firebase_core: ^2.4.1
  firebase_auth: ^4.2.5
  google_sign_in: ^5.4.4  
  google_nav_bar: ^5.0.6
  cloud_firestore: ^4.3.2
  flutter_heatmap_calendar: ^1.0.5
  adaptive_theme: ^3.2.0
  calendar_timeline: ^1.1.2
  get: ^4.6.5
# dev_dependencies:
# flutter_test:
#   sdk: flutter

#   flutter_test:
#     sdk: flutter

  # flutter_lints: ^2.0.0

flutter:
  uses-material-design: true